project(
  'little-scheme', # project name
  'cpp', # language: C++
  default_options : ['warning_level=3', 'cpp_std=c++17']
)

include_dir = include_directories('src')
# directories contain meson.build
subdir('src')
